#MOVE ZEROES TO END
  class solution:
    def move_zero(self,arr):
        l=[]
        for i in range(0,len(arr)):
            if arr[i]!=0:
                l.append(arr[i])
        while len(l) < len(arr):
            l.append(0)
        return l
s=solution()
print(s.move_zero([0,1,0,2]))

#linear search
          class solution:
    def linear_search(self,arr):
        key=12
        for i in range(0,len(arr)):
            if arr[i]==key:
                return i
        return -1
s=solution()
print(s.linear_search([12,23,345]))

#union sorted array.
class Solution:
    def union_sorted(self):
        arr1=list(map(int,input("Enter the elements").split()))
        print(arr1)
        arr2=list(map(int,input("Enter the elements").split()))
        print(arr2)
        s=[]
        for i in range(0,len(arr1)):
            s.append(arr1[i])
        for j in range(0,len(arr2)):
            s.append(arr2[j])
        s=set(s)
        print(s)
        print("union of sorted array",s)
        for i in range(0,len(arr1)):
            for j in range(i+1,len(arr1)):
                if arr1[i]!=arr1[j]:
                    return i ,"distinct elements in arr1"   
                
        for i in range(0,len(arr2)):
            for j in range(i+1,len(arr2)):
                if arr2[i]!=arr2[j]:
                    return i, "distinct elements in arr2" 
s=Solution()
print(s.union_sorted())
        
        
         
            
        
    
