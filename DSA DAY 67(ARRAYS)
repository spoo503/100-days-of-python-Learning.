#we need to find maximum of two cinsectuve elements.
#if arr=[1,1,0,1,1,1]
#consecutive menas it must be continous
#temp=arr[0]
class Solution:
    def max_elements(self,arr):
        count=1
        Max=1
        for i in range(1,len(arr)):
            if arr[i]==arr[i-1]:
                count+=1
            else:
                count=1
            Max=max(count,Max)
        return Max
s=Solution()
print(s.max_elements([1,1,1,0,1,1]))
          
b. CONSECUTIVE OF 1 
  #we need to find maximum of two cinsectuve elements.
#if arr=[1,1,0,1,1,1]
#consecutive menas it must be continous AND 1
#temp=arr[0]
class Solution:
    def max_elements(self,arr):
        count=0
        Max=0
        for i in range(0,len(arr)):
            if arr[i]==1:
                count+=1
            else:
                count=0
            Max=max(count,Max)
        return Max
s=Solution()
print(s.max_elements([1,1,1,0,1,1]))

Find the number that appears once, and the other numbers twice

Problem Statement: Given a non-empty array of integers arr, every element appears twice except for one. Find that single one.

Examples
Example 1:
Input Format: arr[] = {2,2,1}
Result: 1
Explanation: In this array, only the element 1 appear once and so it is the answer.

  class Solution:
    def once_Element(self, arr):
        for i in range(len(arr)):
            count = 0

            for j in range(len(arr)):
                if arr[i] == arr[j]:
                    count += 1

            if count == 1:
                return arr[i]

        return -1


s = Solution()
print(s.once_Element([2, 2, 1]))

Longest Subarray with given Sum K(Positives)


41

Problem Statement: Given an array nums of size n and an integer k, find the length of the longest sub-array that sums to k. If no such sub-array exists, return 0.

class Solution:
    def longest_subarray(self, arr, k):
        max_length = 0

        for i in range(len(arr)):
            sum = 0

            for j in range(i, len(arr)):
                sum += arr[j]

                if sum == k:
                    length = j - i + 1
                    max_length = max(max_length, length)

        return max_length


s = Solution()

print(s.longest_subarray([10, 5, 2, 7, 1, 9], 15))
