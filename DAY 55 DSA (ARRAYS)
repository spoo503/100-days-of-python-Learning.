-----------------------------------------------------FIRST AND LAST ELEMENT--------------------------------------------------------------------------------------
HERE WE NEED TO FIND THE FIRSDST AND LAST OF THE ELEMENT OF THE ARRAY 
A=[3,4,4,4,4,4,4,4,4,6]
HERE WE NEED TO FIND THE FIRS INDEX AND LAST INDEX OF THE ARRAY.
SO,
TO FIND FIRST ELEMENT :
*DO BINARY  SEARCH
while(l<=h):
    m=(l+h)//2
    if key==a[m]:
        result[0]=m
        h=m-1
    elif a[m]<key:
        l=m+1
    else:
        h=m-1
TO FIND LAST ELEMENT OF THE ARRAY:
while(l<=h):
    m=(l+h)//2
    if key==a[m]:
        result[1]=m
        L=m+1
    elif a[m]<key:
        l=m+1
    else:
        h=m-1
combine and the CODE WOULD BE:
def search_range(num,target):
    l,h,m=0,len(a)-1,0
    result=[-1,-1]
    while(l<=h):
        m=(l+h)//2
        if target==nums[m]:
            result[0]=mid
            h=m-1
        elif target<nums[m]:
            l=m+1
        else:
            h=m-1
    l,h,m=0,len(a)-1,0
    while(l<=h):
        m=(l+h)//2
        if target==nums[m]:
            result[0]=mid
            l=m+1
        elif target<nums[m]:
            l=m+1
        else:
            h=m-1
    return result
def main():
    a=[3,4,4,4,4,4,4,4,6]
    print(search_range(nums,target))
if __name__=='__main__':
    main()
#l=0,h=8,m=4--4
#l=0,h=3,m=1---4
#l=0,h=0,m=0---3------out ofthe loop
#l=1,h=-1,m=0--3----out of the loop
#l=0,h=0,m=0----

________________________________________________________________________________________________________________________________________       
